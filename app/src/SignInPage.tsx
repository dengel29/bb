import { PageContainer } from "./PageContainer";
import SignIn from "./SignIn";

export const SignInPage = (): JSX.Element => {
  return (
    <PageContainer title={"Sign In Page"}>
      <SignIn />
    </PageContainer>
  );
};
